spring.application.name=EnterpriseManagementSystem

#-------------------
# Spring Boot requires either:
#
# - .properties format OR
# - .yml (YAML) format — not both together.
#
# For .yml file use this:
#
# spring:
#   application:
#     name: restapi
#
# datasource:
#   url: jdbc:sqlserver://localhost:1433;encrypt=true;trustServerCertificate=true;databaseName=loginedUser
#   username: mohdtest
#   password: Btech@2k21
#   driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
#
# jpa:
#   show-sql: true
#-------------------

# Database configuration
 spring.datasource.url=jdbc:sqlserver://localhost:1433;encrypt=true;trustServerCertificate=true;databaseName=loginedUser
 spring.datasource.username=mohdtest
 spring.datasource.password=Btech@2k21
 spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA settings
 spring.jpa.show-sql=true
 spring.jpa.hibernate.ddl-auto=update

# Enable debug mode for troubleshooting
 debug=true